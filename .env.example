# KLineLens 环境配置模板
#
# 复制此文件为 .env 并根据需要修改：
#   cp .env.example .env
#
# 注意：.env 文件不应提交到 Git

# ============ Provider 配置 ============
# 数据源选择：yahoo (默认，免费) | polygon | twelvedata
# MVP 仅支持 yahoo，其他 provider 将在 V1 实现
PROVIDER=yahoo

# ============ 缓存配置 ============
# K 线数据缓存时间（秒）
# 建议 60 秒以减少对数据源的请求频率
CACHE_TTL=60

# ============ 端口配置 ============
# API 端口（默认 8000）
API_PORT=8000

# Web 端口（默认 3000）
WEB_PORT=3000

# ============ 刷新频率 ============
# 前端自动刷新间隔（秒）
# 建议不低于 60 秒，避免触发 Yahoo Finance 限流
REFRESH_SECONDS=60

# ============ 日志级别 ============
# DEBUG | INFO | WARNING | ERROR
LOG_LEVEL=INFO

# ============ 时区配置 ============
TIMEZONE=UTC

# ============ 可选：付费 Provider API Keys ============
# 如果使用 Polygon.io（需要 V1）
# POLYGON_API_KEY=

# 如果使用 TwelveData（需要 V1）
# TWELVEDATA_API_KEY=
